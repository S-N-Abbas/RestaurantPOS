<UserControl x:Class="RestaurantPOS.UI.Views.Cover.CoverSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestaurantPOS.UI.Views.Cover"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="#AA000000">

        <Border Width="420"
                Background="White"
                CornerRadius="14"
                Padding="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <TextBlock Text="Select Covers"
                           FontSize="22"
                           FontWeight="Bold"
                           Margin="0,0,0,20" />

                <!-- ADULTS -->
                <Grid Grid.Row="1"
                      Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Adults"
                               VerticalAlignment="Center"
                               FontSize="16" />

                    <Button Grid.Column="1"
                            Content="−"
                            Width="32"
                            Margin="6,0"
                            Command="{Binding DecreaseAdultsCommand}" />

                    <TextBox Grid.Column="2"
                             Text="{Binding AdultCovers}"
                             Width="50"
                             FontSize="16"
                             GotFocus="AdultsBox_GotFocus"
                             HorizontalContentAlignment="Center" />

                    <Button Grid.Column="3"
                            Content="+"
                            Width="32"
                            Margin="6,0"
                            Command="{Binding IncreaseAdultsCommand}" />
                </Grid>

                <!-- CHILDREN -->
                <Grid Grid.Row="2"
                      Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Children"
                               VerticalAlignment="Center"
                               FontSize="16" />

                    <Button Grid.Column="1"
                            Content="−"
                            Width="32"
                            Margin="6,0"
                            Command="{Binding DecreaseChildrenCommand}" />

                    <TextBox Grid.Column="2"
                             Text="{Binding ChildCovers}"
                             Width="50"
                             FontSize="16"
                             GotFocus="ChildrenBox_GotFocus"
                             HorizontalContentAlignment="Center" />

                    <Button Grid.Column="3"
                            Content="+"
                            Width="32"
                            Margin="6,0"
                            Command="{Binding IncreaseChildrenCommand}" />
                </Grid>

                <!-- KEYPAD -->
                <UniformGrid Grid.Row="3"
                             Columns="3"
                             Rows="4"
                             Margin="0,0,0,16">

                    <Button Content="1"
                            Command="{Binding AppendDigitCommand}"
                            CommandParameter="1" />
                    <Button Content="2"
                            Command="{Binding AppendDigitCommand}"
                            CommandParameter="2" />
                    <Button Content="3"
                            Command="{Binding AppendDigitCommand}"
                            CommandParameter="3" />

                    <Button Content="4"
                            Command="{Binding AppendDigitCommand}"
                            CommandParameter="4" />
                    <Button Content="5"
                            Command="{Binding AppendDigitCommand}"
                            CommandParameter="5" />
                    <Button Content="6"
                            Command="{Binding AppendDigitCommand}"
                            CommandParameter="6" />

                    <Button Content="7"
                            Command="{Binding AppendDigitCommand}"
                            CommandParameter="7" />
                    <Button Content="8"
                            Command="{Binding AppendDigitCommand}"
                            CommandParameter="8" />
                    <Button Content="9"
                            Command="{Binding AppendDigitCommand}"
                            CommandParameter="9" />

                    <Button Content="C"
                            Command="{Binding ClearCommand}" />
                    <Button Content="0"
                            Command="{Binding AppendDigitCommand}"
                            CommandParameter="0" />
                    <Button Content="←"
                            Command="{Binding BackspaceCommand}" />
                </UniformGrid>

                <!-- CONFIRM -->
                <Button Grid.Row="4"
                        Content="Confirm Covers"
                        Height="48"
                        FontSize="16"
                        FontWeight="Bold"
                        Background="#16A34A"
                        Foreground="White"
                        Command="{Binding ConfirmCommand}" />
            </Grid>
        </Border>
    </Border>
</UserControl>
